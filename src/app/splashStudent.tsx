import WineCarousel from "./components/WineCarousel";

const SplashStudent: React.FC = () => {
  return (
    <section id="student" className="">
      <WineCarousel />
    </section>
  );
};

export default SplashStudent;
